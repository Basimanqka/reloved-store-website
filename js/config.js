/* =====================================================================
   SITE CONFIG — fill these two values in after creating your Supabase
   project (see SETUP.md, step 2). Both are PUBLIC values that are safe
   to ship in client-side code — they do not grant write access on their
   own; that's controlled by Row Level Security + Supabase Auth.
   ===================================================================== */

window.SITE_CONFIG = {
    SUPABASE_URL: "https://ozwydlffpvrcgnhdcjvw.supabase.co",
    SUPABASE_ANON_KEY: "sb_publishable_wyBLx6NPlR8uk-jC-qyupQ_FeQzE37l",
};
